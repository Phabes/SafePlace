import { View } from "react-native";
import { Typography } from "../../../components";

export const Dashboard = () => {
  return (
    <View>
      <Typography text="Dashboard" />
    </View>
  );
};
