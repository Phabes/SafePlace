import { FC } from "react";
import { Typography } from "../../Typography";

type UserNavigationProps = {};

export const UserNavigation: FC<UserNavigationProps> = () => {
  return <Typography text="User" />;
};
